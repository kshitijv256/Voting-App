<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Voter Login</title>
  </head>

  <body>
    <div class="bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 font-sans text-2xl">
      <div class="flex flex-row justify-between pr-4">
        <h1 class="w-3/5 p-5 text-white">Login to vote in election: <%= electionName %></h1>
      </div>
    </div>
    <div class="flex flex-col items-center">
      <div class="h-1/6 text-white">.</div>
      <div class="w-1/3 shadow-xl p-4 mt-4 rounded-md bg-purple-200">
        <form action="/e/<%= electionURL %>/start" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="voterID">VoterID</label>
            <input
              name="voterID"
              type="text"
              class="rounded border-2 border-indigo-500 p-2 w-full text-gray-900"
            />
          </div>
          <div class="py-3">
            <label for="password">Password</label>
            <input
              name="password"
              type="password"
              class="rounded border-2 border-indigo-500 p-2 w-full text-gray-900"
            />
          </div>
          <input type="hidden" name="electionURL" value="<%= electionURL %>" />
          <div class="py-3 flex flex-row">
            <button
              type="submit"
              class="py-2 px-6 rounded font-medium bg-indigo-400 hover:bg-indigo-500 text-white"
            >
              Sign In
            </button>
          </div>
        </form>
      </div>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <%- include('error', {message: messages.error[i]}) %>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>
</html>
