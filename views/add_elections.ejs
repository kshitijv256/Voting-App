<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Add Election</title>
  </head>
  <body>
    <div class="bg-blue-400 font-sans text-2xl">
      <div class="flex flex-row justify-between pr-4">
        <h1 class="w-3/5 p-5 text-white">Got new Election?</h1>
        <div class="flex flex-row justify-end">
          <div class="bg-blue-400 hover:bg-blue-300 cursor-pointer flex items-center">
            <a href="/elections" class="text-white px-2">
              All Elections
            </a>
          </div>
          <div class="bg-blue-400 hover:bg-blue-300 cursor-pointer flex items-center">
            <a href="/logout" class="text-white px-2">
              Logout
            </a>
          </div>
        </div>
      </div>
    </div>
    <div class="flex flex-row justify-center">
      <div class="shadow-xl p-4 mt-4 rounded-md w-3/5">
        <h1 class="text-2xl font-bold mb-4">Add Election</h1>
        <form action="/elections" method="post">
          <div class="flex-auto">
            <label for="title">Title</label>
            <input
              type="text"
              name="title"
              id="title"
              class="rounded w-full p-2 border-2 border-blue-300"
            />
          </div>
          <div class="flex-auto">
            <label for="description">Description</label>
            <input
              type="text"
              name="description"
              id="description"
              class="rounded w-full p-2 border-2 border-blue-300"
            />
            <input type="hidden" name="adminId" value="<%= adminId %>" />
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          </div>
          <div class="flex flex-row justify-end w-full">
            <button
              class="bg-blue-400 p-2 rounded m-2 hover:bg-blue-500 text-white"
              type="submit"
            >
              Add
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
